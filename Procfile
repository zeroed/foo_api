# Sinatra Procfile
# web: bundle exec ruby web.rb -p $PORT
# Grape Procfile (build on top of Rack) 
web: bundle exec rackup config.ru -p $PORT
# ... and using ruby as caller
# web: bundle exec ruby server.rb -sv -e prod -p $PORT
